// to get a better understanding

// % modulo - check even or odd

const checkNum = (n) => {
    if (n % 2 === 0) {
        console.log(n + " is even");
    } else {
        console.log(n + " is odd");
    }
}

checkNum(8);
checkNum(1);

// % modulo - iterate(repetition) an array 
const arrayLength = 20;
let currentIndex = 4;
let nextIndex =  (currentIndex + 1) % arrayLength;

nextIndex; 

// % modulo cycle through an array indices

const arr = ["cat", "dog", "bird", "hamster", "reptile", "fish"];

for (let i = 0; i < 10; i++) {
    console.log(arr[i % arr.length]);
}

// write integer between 1 - 10 
//user is prompted to input "guess Number"
//displays "Good Work" matches
//otherwise not matched


function random
	const guessNum())
		guessNum.Math.random(Math)


